---
// Shared shell for the static legal pages (/terms, /privacy). Same flat dark
// palette and Geist type as the landing page, tuned for long-form prose.
import BaseHead from "../components/BaseHead.astro";
import Brand from "../components/Brand.astro";
import Footer from "../components/Footer.astro";

interface Props {
	title: string;
	description: string;
	path: string;
}
const { title, description, path } = Astro.props;
---

<html lang="en">
  <head>
    <BaseHead preloadSans />
    <title>{title} · uploads.sh</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={`https://uploads.sh${path}`} />
    <style>
      * { box-sizing: border-box; }
      body {
        margin: 0;
        min-height: 100dvh;
        background: var(--bg);
        color: var(--fg);
        font: 15.5px/1.7 var(--sans);
        padding: 48px 24px 64px;
      }
      main { width: min(680px, 100%); margin: 0 auto; }
      .brandbar { margin-bottom: 30px; }
      h1 {
        font-size: clamp(26px, 5vw, 34px);
        font-weight: 600;
        letter-spacing: -0.015em;
        line-height: 1.25;
        margin: 20px 0 4px;
      }
      .effective { color: var(--muted); font-size: 13px; margin: 0 0 36px; }
      article h2 {
        font-size: 19px;
        font-weight: 600;
        letter-spacing: -0.01em;
        margin: 40px 0 10px;
        padding-top: 22px;
        border-top: 1px solid var(--line);
      }
      article h3 { font-size: 15px; font-weight: 600; color: var(--fg); margin: 24px 0 6px; }
      article p, article li { color: var(--body); }
      article strong { color: var(--fg); }
      article ul { padding-left: 22px; }
      article li { margin: 8px 0; }
      article a {
        color: var(--fg);
        text-decoration: underline;
        text-decoration-color: color-mix(in srgb, var(--accent) 55%, transparent);
        text-underline-offset: 3px;
      }
      article a:hover, article a:focus-visible { color: var(--accent); outline: none; }
      article code {
        background: var(--panel);
        border: 1px solid var(--line);
        border-radius: 5px;
        padding: 1px 6px;
        font: 0.88em var(--mono);
        font-variant-ligatures: none;
        font-feature-settings: "calt" 0, "liga" 0;
      }
    </style>
  </head>
  <body>
    <main>
      <div class="brandbar"><Brand /></div>
      <article>
        <slot />
      </article>
      <Footer />
    </main>
  </body>
</html>
