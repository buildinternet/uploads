---
/**
 * Signed-in entry. Login / post-invite land here; bounce into the workspaces
 * index (which auto-opens a single membership). CLI setup lives under
 * /account/developers — overview was not earning a top-level nav slot.
 */
export const prerender = false;

return Astro.redirect("/account/workspaces");
---
