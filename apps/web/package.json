{
  "name": "@uploads/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && node --env-file-if-exists=../../.env ../api/node_modules/wrangler/bin/wrangler.js deploy",
    "typecheck": "astro check || true"
  },
  "dependencies": {
    "astro": "^7.0.6"
  }
}
