{
  "name": "@uploads/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:agent-skills": "node ./scripts/generate-agent-skills.mjs",
    "predev": "pnpm run generate:agent-skills",
    "prebuild": "pnpm run generate:agent-skills",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "pnpm run build && node --env-file-if-exists=../../.env node_modules/wrangler/bin/wrangler.js deploy",
    "typecheck": "astro check"
  },
  "dependencies": {
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^6.0.3",
    "wrangler": "^4.107.0"
  }
}
