---
"@buildinternet/uploads": patch
---

`uploads install` now prints a closing next-step hint when only some agent skills succeed (or all fail), instead of leaving a half-installed state with no guidance after the per-step failure lines.
