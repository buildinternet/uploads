---
"@buildinternet/uploads": minor
---

Add `uploads hook pre-pr-screenshot` for the shared pre-PR screenshot reminder. Claude and Codex plugins run it; `uploads install hooks` wires Grok and Cursor.
