---
"@buildinternet/uploads": minor
---

`uploads put --url` and MCP `contentUrl` fetch a file and upload it. Filename comes from the URL path when omitted. The CLI and stdio MCP also fetch `http://localhost` (hosted MCP does not).
