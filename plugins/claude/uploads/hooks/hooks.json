{
  "description": "Advisory reminder to stage screenshots on uploads.sh before opening a PR that touches UI files.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/pre-pr-screenshot.sh"],
            "timeout": 15,
            "statusMessage": "Checking staged screenshots"
          }
        ]
      }
    ]
  }
}
